const initialState = [
  {
    title: "Some title",
    url: "url url",
  },
  {
    title: "Some title2",
    url: "url url",
  },
];

const topicsReducer = (state = initialState, action) => {
  return state;
};

export default topicsReducer;
