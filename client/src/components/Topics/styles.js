import { makeStyles } from "@material-ui/core/styles";

export const topicsStyles = makeStyles({
  root: {
    maxWidth: 345,
    height: "100%",
  },
  button: {
    marginTop: 10,
  },
  searchContainer: {
    marginBottom: "30px",
    display: "flex",
    flexDirection: "column",
    justifyContent: "space-evenly",
    alignItems: "center",
  },
});
