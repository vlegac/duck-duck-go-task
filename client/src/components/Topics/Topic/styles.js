import { makeStyles } from "@material-ui/core/styles";

export const topicStyles = makeStyles({
  root: {
    maxWidth: 345,
    height: "100%",
  },
  avatar: {
    backgroundColor: "#ff9999",
  },
});
